// swift-tools-version:5.3

import PackageDescription

let package = Package(
    name: "advent_of_code_2021",
    dependencies: [],
    targets: [
        .target(
            name: "day1a",
            dependencies: []),
        .target(
            name: "day1b",
            dependencies: []),
        .target(
            name: "day2a",
            dependencies: []),
        .target(
            name: "day2b",
            dependencies: []),
        .target(
            name: "day3a",
            dependencies: []),
        .target(
            name: "day3b",
            dependencies: []),
        .target(
            name: "day4a",
            dependencies: []),
        .target(
            name: "day4b",
            dependencies: []),
        .target(
            name: "day5a",
            dependencies: []),
        .target(
            name: "day5b",
            dependencies: []),
        .target(
            name: "day6a",
            dependencies: []),
        .target(
            name: "day6b",
            dependencies: []),
        .target(
            name: "day7a",
            dependencies: []),
        .target(
            name: "day7b",
            dependencies: []),
        .target(
            name: "day8a",
            dependencies: []),
        .target(
            name: "day8b",
            dependencies: []),
        .target(
            name: "day9a",
            dependencies: []),
        .target(
            name: "day9b",
            dependencies: []),
        .target(
            name: "day10a",
            dependencies: []),
        .target(
            name: "day10b",
            dependencies: []),
        .target(
            name: "day11a",
            dependencies: []),
        .target(
            name: "day11b",
            dependencies: []),
        .target(
            name: "day12a",
            dependencies: []),
        .target(
            name: "day12b",
            dependencies: []),
        .target(
            name: "day13a",
            dependencies: []),
        .target(
            name: "day13b",
            dependencies: []),
        .target(
            name: "day14a",
            dependencies: []),
        .target(
            name: "day14b",
            dependencies: []),
        .target(
            name: "day15a",
            dependencies: []),
        .target(
            name: "day15b",
            dependencies: []),
        .target(
            name: "day16a",
            dependencies: []),
        .target(
            name: "day16b",
            dependencies: []),
        .target(
            name: "day17a",
            dependencies: []),
        .target(
            name: "day17b",
            dependencies: []),
        .target(
            name: "day18a",
            dependencies: []),
        .target(
            name: "day18b",
            dependencies: []),
    ]
)
